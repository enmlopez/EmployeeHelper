@model IEnumerable<EmployeeHelper.Models.BTModels.BTListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@if (TempData.ContainsKey("SaveBTResult"))
{
    <div class="alert alert-success fade in" role="alert">

        <button type="button" class="close" data-dismiss="alert">
            <span>&times;</span>
        </button>
        @TempData["SaveBTResult"]
    </div>
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BTId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsComplete)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DueOnDate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BTId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsComplete)
            </td>
            <td>
                @{ string formatDate = item.DueOnDate.ToLongDateString();}
                @Html.DisplayFor(modelItem => formatDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.BTId }) |
                @Html.ActionLink("Details", "Details", new { id = item.BTId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.BTId }) |
                @Html.ActionLink("Completed By:", "CompletedBy", new { id = item.BTId })
            </td>
        </tr>
    }

</table>
