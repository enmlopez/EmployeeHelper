@model IEnumerable<EmployeeHelper.Models.OTModels.OverTimeListItem>

@{
    ViewBag.Title = "ChooseOTList";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@if (TempData.ContainsKey("SaveOTWork"))
{
    <div class="alert alert-success fade in" role="alert">

        <button type="button" class="close" data-dismiss="alert">
            <span>&times;</span>
        </button>
        @TempData["SaveOTWork"]
    </div>
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OTId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OTDay)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Days)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsAvailable)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OTId)
            </td>
            <td>
                @{ string formatDate = item.OTDay.ToLongDateString();}
                @Html.DisplayFor(modelItem => formatDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Days)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsAvailable)
            </td>
            <td>
                @Html.ActionLink("Work", "Work", new { id = item.OTId })
            </td>
        </tr>
    }

</table>
<div>
    @Html.ActionLink("Employee List", "Index", "Employee")
</div>
